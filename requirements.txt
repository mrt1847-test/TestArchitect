# TestArchitect Python Dependencies
# pytest and related plugins

# pytest core
pytest>=7.4.0

# pytest JSON report plugin
pytest-json-report>=1.5.0

# Additional pytest plugins (optional)
pytest-html>=3.2.0          # HTML report generation
pytest-cov>=4.1.0            # Code coverage
pytest-xdist>=3.3.0          # Parallel test execution
pytest-rerunfailures>=12.0   # Retry failed tests
pytest-timeout>=2.2.0        # Test timeout management

# Web automation frameworks
playwright>=1.40.0           # Playwright (latest version)
selenium>=4.15.0             # Selenium WebDriver

# Playwright browser driver (auto-installed)
# playwright install command required for separate installation

# Selenium WebDriver management
webdriver-manager>=4.0.0     # WebDriver auto management

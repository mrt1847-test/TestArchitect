# TestArchitect Python 의존성
# pytest 및 관련 플러그인

# pytest 핵심
pytest>=7.4.0

# pytest JSON 리포트 플러그인
pytest-json-report>=1.5.0

# 추가 pytest 플러그인 (선택사항)
pytest-html>=3.2.0          # HTML 리포트 생성
pytest-cov>=4.1.0            # 코드 커버리지
pytest-xdist>=3.3.0          # 병렬 테스트 실행

# 웹 자동화 프레임워크
playwright>=1.40.0           # Playwright (최신 버전)
selenium>=4.15.0             # Selenium WebDriver

# Playwright 브라우저 드라이버 (자동 설치됨)
# playwright install 명령어로 별도 설치 필요

# Selenium WebDriver 관리
webdriver-manager>=4.0.0    # WebDriver 자동 관리

